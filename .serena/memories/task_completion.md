Before finishing changes: rerun ./ci/check.sh from {{cookiecutter.service_name}} (ensures required files/vars + gitleaks), optionally docker compose config for YAML sanity. Update .env.example and README table when adding env vars. Keep Conventional Commit messages; avoid committing real .env or artifacts. If semantic-release used, ensure branch main is clean before ./ci/release.sh.